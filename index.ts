import { reduce } from "simple-ts-lib";
// how do i make it do the following:";
// import { add } from "simple-ts-lib/utils";

export const main = () => {
  console.log('reduce("ADD", 0, 1,2,3): ', reduce("ADD", 0, 1, 2, 3));
};
